
class Border {
   constructor(x, y) {
       this.x = x;
       this.y = y;
       this.width = 16;
       this.height = 16;
   }

   spawn() {

   }

   update() {
       return true;
   }

   draw(context, offset) {
   }
  
}

export default Border;
